# 🤖 خطة مشروع (النسخة 2): نظام تداول متعدد الوكلاء يتشاورون فيما بينهم
## للذهب (XAU/USD) والبيتكوين (BTC/USDT) — Real-Time — بحد أقصى 100 صفقة/يوم — **بدون تدريب وبدون أي API Token**

> **الفكرة الجوهرية:** بدل تدريب نماذج بنفسك، الوكلاء الخمسة هم أدوار (personas) لنموذج لغوي مفتوح المصدر يعمل **محلياً على حاسوبك عبر Ollama**. يتحاورون فعلياً بلغة طبيعية — يعرض كل وكيل تحليله، يعترض الآخر، ويحسم المنسّق — ثم يصدر قرار تداول منظّم (JSON). لا اشتراكات، لا مفاتيح، لا إنترنت وقت الاستدلال.

---

## 1. كيف نستغني عن التدريب؟ ثلاث لبنات جاهزة

| اللبنة | ما هي | التدريب المطلوب منك |
|---|---|---|
| **LLM محلي (Ollama)** | نموذج مفتوح مثل Qwen2.5 أو Llama 3.2 يعمل على الـ RTX 3050 | ❌ صفر — يُحمَّل جاهزاً مرة واحدة |
| **FinBERT جاهز** | نموذج مشاعر مالية مدرَّب مسبقاً (من Hugging Face، يعمل offline) | ❌ صفر — يُستخدم كما هو |
| **مؤشرات فنية (pandas-ta)** | RSI, MACD, ATR… رياضيات بحتة | ❌ لا شيء يُدرَّب أصلاً |

الـ LLM لا يُطعَم أسعاراً خام (النماذج الصغيرة ضعيفة في الأرقام الخام) — بل يُطعَم **حقائق محسوبة مسبقاً**: "RSI=71 على فريم 15 دقيقة، السعر فوق EMA200، خبر CPI بعد 20 دقيقة، مشاعر الأخبار سلبية −0.6". ثم يقوم بما يجيده: **الاستنتاج والنقاش واتخاذ القرار**.

---

## 2. اختيار النموذج المحلي (حسب عتادك: RTX 3050 4GB VRAM + 16GB RAM)

| النموذج | الحجم | السرعة على جهازك | التقييم |
|---|---|---|---|
| **Qwen2.5 3B Instruct (Q4)** | ~2GB | سريع جداً (كله على GPU) | ✅ الخيار الافتراضي — ممتاز في اتباع التعليمات وإخراج JSON |
| Llama 3.2 3B (Q4) | ~2GB | سريع جداً | ✅ بديل جيد |
| Qwen2.5 7B (Q4) | ~4.5GB | متوسط (جزء على CPU/RAM) | ✅ أذكى — استخدمه لجولات التشاور المهمة، مقبول لأن القرارات ≤100/يوم |
| نماذج 13B+ | >8GB | بطيء جداً | ❌ تجاوز إمكانيات الجهاز |

**استراتيجية ذكية:** نموذجان معاً — الـ 3B للفحص السريع المستمر، والـ 7B يُستدعى فقط عند "اجتماع" حقيقي (إشارة قوية أو خبر مهم). Ollama يبدّل بينهما تلقائياً.

---

## 3. الوكلاء الخمسة وآلية التشاور (Deliberation Protocol)

```
   [بيانات لحظية: Binance WS + MT5 + أخبار RSS]
                      │
        ┌─────────────▼──────────────┐
        │  طبقة الحقائق (بدون LLM)   │  ← تعمل كل ثانية: مؤشرات، FinBERT،
        │  Market Snapshot Builder   │     كشف الأحداث → "ملف موجز للسوق"
        └─────────────┬──────────────┘
                      │ (عند إشارة/خبر/فاصل زمني)
        ┌─────────────▼──────────────────────────────┐
        │            🏛️ غرفة التشاور (LLM محلي)       │
        │                                            │
        │  Agent 1 محلل السوق ──┐                    │
        │  Agent 3 محلل الأخبار ─┼─► نقاش ثنائي الاتجاه│
        │  Agent 2 وكيل التداول ─┘   (Bull vs Bear)   │
        │            │                               │
        │  Agent 5 المنسّق: يلخّص الحجج ويقترح قراراً │
        │            │                               │
        │  Agent 4 مدير المخاطر: فيتو (قواعد صلبة،   │
        │            ليس LLM — لا يُقنَع ولا يُتفاوَض) │
        └─────────────┬──────────────────────────────┘
                      │ قرار JSON: {action, size, SL, TP, reasoning}
              [تنفيذ على Binance / MT5]
```

### الأدوار (كل وكيل = system prompt مختلف لنفس النموذج المحلي)

1. **🔵 محلل السوق:** يقرأ لقطة المؤشرات الفنية على عدة فريمات ويقدّم رأياً باتجاه السوق مع درجة ثقة، ويجب أن يذكر حججه.
2. **🟠 محلل الأخبار:** يستلم عناوين الأخبار + نتيجة FinBERT + التقويم الاقتصادي، ويقيّم: هل هناك حدث يقلب الطاولة؟ هل المشاعر تدعم أو تناقض التحليل الفني؟
3. **🟢 وكيل التداول:** يحوّل خلاصة النقاش إلى خطة صفقة ملموسة: دخول، حجم، وقف خسارة، هدف — أو يوصي بعدم التداول.
4. **🔴 مدير المخاطر (بدون LLM — عمداً):** قواعد برمجية صلبة لا تتأثر بالإقناع: عدّاد 100 صفقة/يوم، حد خسارة يومي −3%، حجم أقصى للصفقة، إيقاف قبل الأحداث الكبرى. **هذا الوكيل يجب ألا يكون LLM** حتى لا "يهلوس" في اللحظة الحرجة.
5. **🟣 المنسّق:** يدير الجلسة: يطلب رأي 1 و 3، يشغّل جولة اعتراض (كل وكيل يردّ على الآخر مرة واحدة)، يزن الحجج، يطلب الخطة من 2، ويمرّرها للفيتو عند 4. كل جلسة تُسجَّل كاملة (audit log) — ستقرأ لاحقاً "لماذا" اتُّخذ كل قرار، وهذه ميزة ضخمة على الشبكات العصبية الصمّاء.

### أدوات التنسيق (Frameworks)
- **الخيار الموصى به: بناء يدوي خفيف** — حلقة أسئلة/أجوبة عبر Ollama API المحلي (`http://localhost:11434`) — تحكم كامل وبساطة.
- بدائل جاهزة: **AutoGen** أو **CrewAI** أو **LangGraph** — كلها تدعم Ollama كخلفية بدون أي مفتاح.

---

## 4. التوفيق بين "Real-Time" وبطء الـ LLM

الـ LLM المحلي يحتاج 15–60 ثانية لجولة تشاور كاملة — لا يمكنه التعليق على كل tick. الحل: **فصل الطبقتين**:

- **طبقة اليقظة (كل ثانية، بدون LLM):** تجمع الـ ticks، تحسب المؤشرات، ترصد الأخبار. سريعة جداً.
- **طبقة التشاور (عند الحاجة فقط):** تُستدعى غرفة الاجتماع عندما: تقاطع مؤشرات مهم، خبر عاجل، اقتراب السعر من SL/TP، أو فاصل دوري (مثلاً كل 15 دقيقة).
- **الحماية اللحظية دائماً بدون LLM:** أوامر Stop-Loss توضع في المنصة نفسها (Binance/MT5) — فإذا انهار السوق أثناء "تفكير" الوكلاء، الحماية مفعّلة مسبقاً.

هذا التصميم متناغم تماماً مع قيد **100 صفقة/يوم**: ~100–200 جلسة تشاور يومياً شيء مريح جداً حتى للنموذج 7B على جهازك.

---

## 5. البنية التقنية المحدّثة

| الطبقة | الأداة | ملاحظة |
|---|---|---|
| LLM محلي | **Ollama** + Qwen2.5 3B و 7B | تحميل مرة واحدة، offline، بدون مفتاح |
| مشاعر الأخبار | FinBERT (Hugging Face, offline) | جاهز بدون تدريب |
| بيانات لحظية | `python-binance` (WebSocket) + `MetaTrader5` | مجاني — MT5 يعمل على Windows فقط (مناسب لجهازك) |
| مؤشرات | `pandas-ta` | بدون تدريب |
| تنسيق الوكلاء | Python + Ollama API (أو AutoGen/CrewAI) | حلقة نقاش + إخراج JSON منظّم |
| مدير المخاطر | Python خالص (قواعد) | بدون LLM عمداً |
| Backtesting | `vectorbt` + إعادة تشغيل جلسات التشاور على التاريخ | ضروري رغم عدم التدريب! |
| تخزين | SQLite (صفقات + محاضر الجلسات) + Parquet | خفيف على 16GB RAM |
| مراقبة | Streamlit dashboard | يعرض النقاشات الحية والقرارات والأرباح |

---

## 6. خارطة الطريق المحدّثة (أقصر من نسخة التدريب!)

### المرحلة 0 — التجهيز (يوم)
- [ ] تثبيت Ollama + سحب `qwen2.5:3b-instruct` و `qwen2.5:7b-instruct-q4_K_M`.
- [ ] تثبيت Python 3.11 والمكتبات، حساب Binance Testnet + MT5 Demo.

### المرحلة 1 — طبقة الحقائق (أسبوع)
- [ ] جامعات البيانات: Binance WS، MT5 ticks، أخبار RSS + التقويم الاقتصادي.
- [ ] بنّاء "لقطة السوق" (Market Snapshot): مؤشرات متعددة الفريمات + مشاعر FinBERT → نص موجز يفهمه الـ LLM.

### المرحلة 2 — غرفة التشاور (أسبوع)
- [ ] كتابة system prompts للأدوار الأربعة (1، 2، 3، 5) وضبط بروتوكول النقاش (رأي → اعتراض → قرار).
- [ ] فرض إخراج JSON صارم (Ollama structured outputs) + إعادة المحاولة عند مخرجات فاسدة.
- [ ] بناء مدير المخاطر (قواعد + عدّاد 100 صفقة/يوم في SQLite).

### المرحلة 3 — التقييم على التاريخ (أسبوع–أسبوعان)
- [ ] **Replay Backtest:** إعادة تشغيل جلسات التشاور على بيانات وأخبار تاريخية (لا تدريب — لكن التقييم إجباري: يجب أن نعرف هل النقاشات تنتج قرارات رابحة أصلاً).
- [ ] ضبط العتبات: متى تُستدعى الغرفة؟ ما حد الثقة للتنفيذ؟
- [ ] المقاييس المستهدفة: Profit Factor > 1.3، Max Drawdown < 15%.

### المرحلة 4 — Paper Trading (شهر إجباري)
- [ ] تشغيل حي على Testnet/Demo مع dashboard يعرض محاضر الجلسات لحظياً.
- [ ] مراجعة أسبوعية لمحاضر النقاش: أين أخطأ الوكلاء؟ → تحسين الـ prompts (هذا "التدريب" الوحيد — تحرير نصوص، ليس GPU).

### المرحلة 5 — تشغيل حقيقي تدريجي
- [ ] مبلغ صغير جداً، وكل حدود مدير المخاطر مفعّلة.

---

## 7. المقايضة بصراحة: ماذا تكسب وماذا تخسر بترك التدريب؟

| | مع تدريب (النسخة 1) | بدون تدريب — تشاور (النسخة 2) |
|---|---|---|
| وقت الوصول لنظام يعمل | 6–8 أسابيع | **3–4 أسابيع** ✅ |
| قابلية تفسير القرارات | ضعيفة (صناديق سوداء) | **ممتازة — محضر نقاش لكل قرار** ✅ |
| التحسين المستمر | إعادة تدريب دورية | تحرير prompts فقط ✅ |
| قوة التنبؤ الرقمي | أعلى (نماذج متخصصة في بياناتك) ✅ | أضعف — النموذج الصغير يستنتج ولا يتنبأ رقمياً ⚠️ |
| خطر الهلوسة | لا يوجد | موجود — يُحتوى بـ JSON صارم + فيتو مدير المخاطر ⚠️ |
| سرعة القرار | ميلي ثانية | 15–60 ثانية (كافٍ لـ 100 صفقة/يوم) |

**التوصية:** ابدأ بالنسخة 2 (تشاور بدون تدريب) — أسرع وصولاً وأوضح تفسيراً. وإذا أثبت النظام جدواه على الورق، أضف لاحقاً نموذج LightGBM مدرَّباً كـ"مستشار سادس" يقدّم توقعاً رقمياً داخل الجلسة — تهجين الأفضلَين دون إعادة بناء.

---

## 8. تحذيرات تبقى سارية

1. لا يوجد نظام يضمن الربح — الهدف edge صغير + انضباط مخاطر صارم.
2. النماذج الصغيرة (3B/7B) قد تكون واثقة وهي مخطئة — لذلك الفيتو النهائي **دائماً** لقواعد برمجية صلبة، وأوامر الحماية توضع في المنصة مسبقاً.
3. الـ Backtest إجباري حتى بدون تدريب — عدم التدريب لا يعني عدم التقييم.
4. شهر Paper Trading كامل قبل أي أموال حقيقية.

---

## 9. الخطوة التالية

عند الموافقة: نبدأ المرحلة 0 و 1 — تثبيت Ollama، هيكل المشروع، وجامعات البيانات، ثم أول جلسة تشاور تجريبية بين الوكلاء على بيانات حية.
